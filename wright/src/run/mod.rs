
use super::argparse::ProcessingMode;

pub fn interactive() -> i32 { // the i32 is exit code
    0
}

pub fn interpret(p: ProcessingMode, file: String) -> i32 { // the i32 is exit code
    0
}

pub fn compile(p: ProcessingMode, file: String, opt: u8, output: String) -> i32 { // the i32 is exit code
    0
}